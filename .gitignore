# ─── 本地 AI 工具数据（不纳入版本控制）───
.ace-tool/
.claude/

# ─── 构建产物（将来 build/ 里的打包输出，由 GitHub Actions 自动构建）───
installer/build/dist/
installer/build/*.zip
installer/build/*.exe

# ─── Windows 系统垃圾文件 ───
Thumbs.db
ehthumbs.db
desktop.ini
$RECYCLE.BIN/
*.lnk

# ─── 编辑器临时文件 ───
*.tmp
*.bak
*.swp
*~

# ─── PowerShell 产物 ───
*.psc1
